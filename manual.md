# TaskHub 操作マニュアル

## 目次
1. [業務ガント](#業務ガント)
2. [チーム設定](#チーム設定)

---

# 業務ガント

Slackタスク（期限付きのタスク）と、手動で登録した繰り返し業務を、チームメンバーごとに横並びのガントチャートで確認・管理できる画面です。

---

## ツールバーの使い方

画面上部のツールバーで表示対象や期間を絞り込めます。

| 項目 | 内容 |
|------|------|
| **チーム選択（左側ドロップダウン）** | 親チームを選択します |
| **部署選択（右側ドロップダウン）** | 親チームに子チームがある場合、表示する部署を選択します |
| **今週 / 14日 / 31日 / 月** | 表示する期間を切り替えます |
| **前月 / 次月ボタン** | 「月」表示のとき、月を前後に移動します |
| **ステータスフィルター** | Slackタスクを「進行中のみ」「すべて表示」「完了のみ」で絞り込みます |
| **Slackタスク 表示中 / 非表示** | Slackタスクのバーをガントに表示するか切り替えます |
| **色丸（Slackタスク表示中のとき）** | クリックするとカラーピッカーが開き、**自分の画面のみ** Slackタスクバーの色を変更できます。他のユーザーには影響しません |
| **カテゴリ管理** | 業務カテゴリの追加・編集・削除を行います（後述） |

---

## ガントの見方

### メンバーヘッダー行（灰色の行）

各メンバーの名前・アバター・件数が表示される行です。

- **タスク数** : 業務アイテム数 ＋ Slackタスク数の合計
- **業務追加ボタン（＋）** : そのメンバーに新しい業務を追加します

### 業務行（白い行）

手動で登録した業務です。カテゴリやパターンに応じた色のバーが表示されます。

- 行の左端をドラッグすると、**同じメンバー内で並び順を変更**できます
- バーの右端をドラッグすると**期間を延長／短縮**できます
- バー全体をドラッグすると**開始日を前後に移動**できます

### Slackタスク行（色付きの行）

Slackのタスクボットから取得したタスクです。

- バーの色は自分の設定色（デフォルト: 青）
- **期限切れのタスクは赤**で表示されます
- バー全体をドラッグして**期限日を変更**できます

### 日付ヘッダーの色分け

| 色 | 意味 |
|----|------|
| 赤文字 | 日曜日 |
| 青文字 | 土曜日 |
| **薄赤背景** | 国民の祝日 |
| 太線縦線 | 今日 |

---

## 業務の追加・編集

### 新規追加

メンバー行の右端にある **＋** ボタンをクリックするとフォームが開きます。

| 項目 | 内容 |
|------|------|
| **タイトル** | 業務の名称（必須） |
| **カテゴリ** | プルダウンから選択。選択するとそのカテゴリの色でバーが表示される |
| **色** | カテゴリ未選択の場合に個別で色を指定できます |
| **繰り返しパターン** | 下記4種類から選択 |
| **備考** | 自由記述メモ |

### 繰り返しパターン

| パターン | 内容 |
|---------|------|
| **期間指定** | 開始日〜終了日を指定。指定した日付範囲のセルが塗られます |
| **毎日（平日のみ）** | 土日祝を除く全日に表示されます |
| **毎週** | 表示する曜日（月〜金）をチェックで選択します |
| **毎月** | 日付（1〜31日）をチェックで選択します。**「営業日数換算する」チェックを入れると、土日祝を除いた営業日で数えます**（例: 31日 = その月の最終営業日）|

> **営業日換算の詳細**  
> 27〜31日は月末から逆算して 5〜1 番目の営業日になります。  
> 1〜26日は月初から N 番目の営業日になります。  
> 土日・国民の祝日は自動的に除外されます。

### 編集

業務行の左パネルにある行をクリックするか、行右端の **鉛筆（✎）ボタン** をクリックするとフォームが開きます。

### 複製

業務行右端の **複製（⧉）ボタン** をクリックすると、元の業務の直下に同じ内容のコピーが追加されます。期間指定の場合はセルの塗り情報も複製されます。

### 削除

業務行右端の **削除（×）ボタン** をクリックすると確認ダイアログが表示されます。

---

## 担当者を変更する（業務の移動）

業務行の左端（ハンドル部分）をドラッグして、**別のメンバーのヘッダー行にドロップ**すると担当者を変更できます。ドロップ先メンバーの一覧の末尾に追加されます。

---

## 前月のコピー（月表示のみ）

「月」表示の場合、ツールバーに **「前月からコピー」** ボタンが表示されます（開発中機能）。前月の業務登録内容を現在の月にコピーします。

---

## カテゴリ管理

ツールバーの **「カテゴリ管理」** ボタンをクリックするとパネルが展開します。

- **名前** と **色** を入力して「保存」するとカテゴリを登録できます
- 登録済みカテゴリの行をクリックすると編集できます
- **×** ボタンで削除できます
- カテゴリの色は業務バーの色に反映されます。カテゴリを選択中は個別の色ピッカーは無効になります

---

# チーム設定

チームと部署のメンバー構成を確認・編集できる画面です。

---

## 表示モードと編集モードの切り替え

画面右上の **「チームを組む」ボタン** で編集モードに入ります。**「✓ 編集完了」ボタン** で表示モードに戻ります。

| モード | できること |
|--------|-----------|
| **表示モード** | 部署・チームごとのメンバー一覧を閲覧。名前・役職で検索が可能 |
| **編集モード** | メンバーのチーム追加・削除、役割変更、チーム作成・削除・改名、部署への所属変更 |

---

## 表示モードの使い方

### チーム・部署の表示構造

- **部署**（親チームあり）: 複数のチームをまとめたグループ。チーム名をクリックで折りたたみ
- **独立チーム**（親チームなし）: 部署に属さない単独チーム
- **未所属**: どのチームにも属していないメンバー

### 検索

画面上部の検索ボックスに名前や役職を入力するとリアルタイムで絞り込まれます。

---

## 編集モードの使い方

### 新しいチームを追加する

右上の **「＋ チームを追加」ボタン** をクリックするとチームが作成されます。カードのチーム名をクリックして名前を変更できます。

### メンバーをチームに追加する

1. **「未所属」** エリアに表示されているメンバーチップをドラッグします
2. 追加したいチームカードの上にドロップします
3. カードにハイライト（青い点線枠）が出ているときに離すと追加されます

> 検索ボックスでメンバー名を絞り込んでから操作するとスムーズです。

### メンバーをチームから外す

チームカード内のメンバーチップ右端の **×** をクリックします。

### 役割（ロール）を変更する

メンバーチップ右端のロールバッジ（例: 「メンバー ▾」）をクリックするとドロップダウンが開き、役割を変更できます。

| 役割 | 表示色 |
|------|--------|
| アドミン | 赤 |
| 部署リーダー | 紫 |
| チームリーダー | 青 |
| サブリーダー | 水色 |
| メンバー | グレー |

### チームを別の部署に移動する

チームカード下部の **「所属部署」** ドロップダウンで親チーム（部署）を選択します。「なし（独立チーム）」を選ぶと部署に属さない独立チームになります。

### チーム名を変更する

チームカード内のチーム名をクリックするとテキスト入力になります。Enterキーまたはクリックアウトで確定します。

### チームを削除する

チームカード右上の **×ボタン** をクリックすると確認ダイアログが表示されます。削除するとチームに属していたメンバーは「未所属」に戻ります。

### メンバーを組織図から非表示にする

未所属パネル内のメンバーチップ右上の小さな **×** をクリックすると、そのメンバーを一覧から非表示にできます（退職者など）。

### 検索（2ボックス）

編集モード上部に青と緑の2つの検索ボックスがあります。**OR検索** で、どちらかに一致するチーム・メンバーを含むカードだけを表示できます。チームを移動するとき、移動元と移動先の両方を素早く探すのに便利です。

---

## 変更の保存タイミング

チーム設定の変更（メンバー追加・削除・ロール変更・チーム作成等）は**操作のたびに自動保存**されます。画面上部に「保存中…」と表示されている間は通信中です。

---

*最終更新: 2026-04-14*
